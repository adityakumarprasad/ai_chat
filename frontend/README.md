# Frontend Notes

The main project documentation now lives in the repository root README.

Use these commands inside `frontend/`:

```bash
npm run dev
npm run build
```

Required env:

```env
VITE_API_BASE_URL=http://localhost:5000
```
